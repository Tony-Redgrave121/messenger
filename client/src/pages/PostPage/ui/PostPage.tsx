import { PostHeader } from '@widgets/Header';
import { Post } from '@widgets/Messenger';

const PostPage = () => {
    return (
        <Post>
            <PostHeader />
        </Post>
    );
};

export default PostPage;
